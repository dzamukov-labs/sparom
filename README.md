# Сайт «С лёгким паром» на Astro

Готовые бани под ключ. Тёмный минималистичный дизайн.

## Дизайн

- **Цветовая схема:** тёмный фон (#0D0D0D) + янтарный акцент (#D4915E)
- **Шрифты:** Cormorant Garamond (заголовки) + Outfit (текст)
- **Стиль:** минималистичный, современный

## 🚀 Быстрый старт

```bash
# Установить зависимости
npm install

# Запустить dev-сервер
npm run dev
```

Откройте http://localhost:4321

## 📁 Структура проекта

```
/
├── src/
│   └── pages/              # Страницы сайта
│       ├── index.astro     # Главная
│       ├── about.astro     # О компании
│       └── proekty/
│           └── 2-3x4.astro # Страница проекта
├── public/                 # Статические файлы
│   └── images/
├── api/                    # Vercel Serverless Functions
│   └── prices.js           # Google Sheets API
├── bot/                    # Telegram бот с админкой
│   ├── index.js
│   └── admin.html          # Админка (запускать отдельно)
└── lp/                     # ⚠️ НЕ ТРОГАТЬ! Лендинги
```

## ⚠️ Важно!

### Папка `/lp` - НЕ ТРОГАТЬ!
Лендинги работают **независимо** от Astro.

### Папка `/bot` - отдельный сервер
Бот и админка работают отдельно. Запуск:
```bash
cd bot && npm install && node index.js
```
Админка: `http://localhost:3000/admin`

## 📝 Команды

| Команда | Действие |
| --- | --- |
| `npm run dev` | Dev-сервер на `localhost:4321` |
| `npm run build` | Production сборка |
| `npm run preview` | Превью сборки |

## 🎨 CSS переменные

```css
--bg-dark: #0D0D0D;
--bg-medium: #141414;
--bg-light: #1A1A1A;
--accent: #D4915E;
--accent-hover: #E5A26F;
--text: #F5F5F0;
--text-secondary: #A8A8A0;
--border: #2A2A2A;
```

## 📄 Страницы

- `/` — Главная (8 проектов, бегущая строка регионов)
- `/about` — О компании (история, команда)
- `/proekty/2-3x4` — Страница проекта (галерея, конфигуратор, сравнение)

## 🔧 Деплой

Vercel автоматически собирает Astro. Переменные окружения для Google Sheets:
- `GOOGLE_SERVICE_ACCOUNT_EMAIL`
- `GOOGLE_PRIVATE_KEY`
- `GOOGLE_PROJECT_ID`

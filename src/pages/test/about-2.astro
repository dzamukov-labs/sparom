---
// О компании — Смелая версия
// Порядок блоков сохранён, тексты сохранены
// Дизайн: современный, драматичный, но лёгкий (2G-friendly)
// Только CSS-анимации, минимум JS
---
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>О компании — С лёгким паром</title>
    <meta name="description" content="С 2014 года создаём бани, которые служат долгие годы. Производство в Пестово, Новгородская область. Доставка по всей России.">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;500;600&family=Outfit:wght@300;400;500&display=swap" rel="stylesheet">
    <style>
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

        :root {
            --bg-dark: #0D0D0D;
            --bg-medium: #151515;
            --bg-light: #1A1A1A;
            --text: #FAFAFA;
            --text-secondary: #A8A8A8;
            --text-muted: #707070;
            --accent: #D4915E;
            --accent-hover: #E5A36E;
            --border: #2A2A2A;
        }

        html { font-size: 16px; scroll-behavior: smooth; }

        body {
            font-family: 'Outfit', -apple-system, sans-serif;
            font-weight: 300;
            color: var(--text);
            background: var(--bg-dark);
            line-height: 1.6;
            overflow-x: hidden;
        }

        h1, h2, h3, .serif { font-family: 'Cormorant Garamond', Georgia, serif; font-weight: 400; }
        a { color: inherit; text-decoration: none; }
        img { max-width: 100%; height: auto; display: block; }

        /* ========== HEADER ========== */
        .header {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 100;
            padding: 1.25rem 1.5rem;
            background: transparent;
            transition: background 0.3s;
        }

        .header.scrolled {
            background: rgba(13,13,13,0.95);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
        }

        .header-inner {
            max-width: 1600px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.25rem;
            font-weight: 600;
            letter-spacing: 0.05em;
            color: var(--text);
        }

        .header-nav {
            display: none;
            gap: 2.5rem;
        }

        .header-nav a {
            font-size: 0.8125rem;
            font-weight: 400;
            letter-spacing: 0.1em;
            text-transform: uppercase;
            color: var(--text-secondary);
            transition: color 0.3s;
        }

        .header-nav a:hover {
            color: var(--accent);
        }

        .header-phone {
            font-size: 0.875rem;
            color: var(--text-secondary);
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .header-phone::before {
            content: '';
            width: 6px;
            height: 6px;
            background: var(--accent);
            border-radius: 50%;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.4; }
        }

        /* ========== HERO — Oversized Typography ========== */
        .hero {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 8rem 2rem 4rem;
            position: relative;
        }

        .hero::before {
            content: '2014';
            position: absolute;
            top: 50%;
            right: -5%;
            transform: translateY(-50%);
            font-family: 'Cormorant Garamond', serif;
            font-size: clamp(15rem, 40vw, 50rem);
            font-weight: 400;
            color: var(--bg-elevated);
            line-height: 1;
            pointer-events: none;
            z-index: 0;
        }

        .hero-content {
            position: relative;
            z-index: 1;
            max-width: 900px;
        }

        .hero-eyebrow {
            font-size: 0.75rem;
            font-weight: 500;
            letter-spacing: 0.3em;
            text-transform: uppercase;
            color: var(--accent);
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .hero-eyebrow::before {
            content: '';
            width: 3rem;
            height: 1px;
            background: var(--accent);
        }

        .hero h1 {
            font-family: 'Cormorant Garamond', serif;
            font-size: clamp(2.5rem, 7vw, 5.5rem);
            font-weight: 400;
            line-height: 1.1;
            letter-spacing: -0.02em;
        }

        .hero h1 span {
            display: block;
        }

        .hero h1 .highlight {
            color: var(--accent);
        }

        .hero-sub {
            margin-top: 3rem;
            padding-left: 4rem;
            border-left: 1px solid var(--border);
        }

        .hero-sub p {
            font-size: 1.125rem;
            color: var(--text-secondary);
            max-width: 400px;
        }

        /* ========== STATS — Horizontal Strip ========== */
        .stats {
            background: var(--bg-medium);
            border-top: 1px solid var(--border);
            border-bottom: 1px solid var(--border);
            overflow: hidden;
        }

        .stats-track {
            display: flex;
            animation: scroll 30s linear infinite;
        }

        @keyframes scroll {
            0% { transform: translateX(0); }
            100% { transform: translateX(-50%); }
        }

        .stats-track:hover {
            animation-play-state: paused;
        }

        .stat-item {
            flex-shrink: 0;
            padding: 3rem 4rem;
            display: flex;
            align-items: baseline;
            gap: 1rem;
            border-right: 1px solid var(--border);
        }

        .stat-number {
            font-family: 'Cormorant Garamond', serif;
            font-size: 4rem;
            font-weight: 400;
            color: var(--accent);
            line-height: 1;
        }

        .stat-label {
            font-size: 0.8125rem;
            color: var(--text-secondary);
            text-transform: uppercase;
            letter-spacing: 0.1em;
            max-width: 120px;
        }

        /* ========== FOUNDER — Overlapping Layout ========== */
        .founder {
            padding: 10rem 2rem;
            position: relative;
        }

        .founder-inner {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            gap: 4rem;
        }

        .founder-photo-wrap {
            position: relative;
        }

        .founder-photo {
            aspect-ratio: 4/5;
            overflow: hidden;
            position: relative;
        }

        .founder-photo img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            filter: grayscale(30%);
            transition: filter 0.5s;
        }

        .founder-photo:hover img {
            filter: grayscale(0%);
        }

        .founder-photo::after {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(to top, var(--bg) 0%, transparent 30%);
        }

        .founder-label-box {
            position: absolute;
            bottom: -2rem;
            left: -1rem;
            background: var(--accent);
            color: var(--bg);
            padding: 1.5rem 2rem;
            z-index: 2;
        }

        .founder-label-box span {
            display: block;
            font-size: 0.6875rem;
            letter-spacing: 0.2em;
            text-transform: uppercase;
            opacity: 0.7;
        }

        .founder-label-box strong {
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.5rem;
            font-weight: 500;
        }

        .founder-content {
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .founder-quote {
            font-family: 'Cormorant Garamond', serif;
            font-size: clamp(1.5rem, 3vw, 2.25rem);
            font-style: italic;
            line-height: 1.5;
            color: var(--text);
            margin-bottom: 3rem;
            position: relative;
            padding-left: 2rem;
        }

        .founder-quote::before {
            content: '"';
            position: absolute;
            left: 0;
            top: -0.5rem;
            font-size: 4rem;
            color: var(--accent);
            line-height: 1;
        }

        .founder-text {
            columns: 1;
            column-gap: 3rem;
        }

        .founder-text p {
            font-size: 0.9375rem;
            color: var(--text-secondary);
            line-height: 1.9;
            margin-bottom: 1.25rem;
        }

        .founder-signature {
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.25rem;
            color: var(--accent);
            margin-top: 2rem;
        }

        .founder-btn {
            display: inline-flex;
            align-items: center;
            gap: 1rem;
            margin-top: 3rem;
            padding: 1.25rem 2.5rem;
            background: transparent;
            border: 1px solid var(--accent);
            color: var(--accent);
            font-family: 'Outfit', sans-serif;
            font-size: 0.875rem;
            font-weight: 400;
            letter-spacing: 0.05em;
            cursor: pointer;
            transition: all 0.3s;
        }

        .founder-btn:hover {
            background: var(--accent);
            color: var(--bg);
        }

        /* ========== TIMELINE — Vertical Line ========== */
        .timeline {
            padding: 8rem 2rem;
            background: var(--bg-medium);
        }

        .timeline-inner {
            max-width: 900px;
            margin: 0 auto;
        }

        .timeline h2 {
            font-family: 'Cormorant Garamond', serif;
            font-size: clamp(2rem, 5vw, 3.5rem);
            text-align: center;
            margin-bottom: 5rem;
        }

        .timeline-line {
            position: relative;
            padding-left: 3rem;
        }

        .timeline-line::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 1px;
            background: linear-gradient(to bottom, var(--accent), var(--border));
        }

        .timeline-item {
            position: relative;
            padding-bottom: 4rem;
        }

        .timeline-item:last-child {
            padding-bottom: 0;
        }

        .timeline-item::before {
            content: '';
            position: absolute;
            left: -3rem;
            top: 0.5rem;
            width: 9px;
            height: 9px;
            background: var(--bg-medium);
            border: 2px solid var(--accent);
            border-radius: 50%;
            transform: translateX(-50%);
        }

        .timeline-year {
            font-family: 'Cormorant Garamond', serif;
            font-size: 3rem;
            color: var(--accent);
            line-height: 1;
            margin-bottom: 1rem;
        }

        .timeline-text {
            font-size: 1rem;
            color: var(--text-secondary);
            line-height: 1.8;
            max-width: 500px;
        }

        /* ========== TEAM — Minimal Cards ========== */
        .team {
            padding: 8rem 2rem;
        }

        .team-inner {
            max-width: 1000px;
            margin: 0 auto;
        }

        .team h2 {
            font-family: 'Cormorant Garamond', serif;
            font-size: clamp(2rem, 5vw, 3rem);
            text-align: center;
            margin-bottom: 5rem;
        }

        .team-grid {
            display: grid;
            gap: 2rem;
        }

        .team-card {
            display: flex;
            align-items: center;
            gap: 2rem;
            padding: 2rem;
            background: var(--bg-light);
            border: 1px solid var(--border);
            transition: border-color 0.3s;
        }

        .team-card:hover {
            border-color: var(--accent);
        }

        .team-photo {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            overflow: hidden;
            flex-shrink: 0;
            position: relative;
        }

        .team-photo::after {
            content: '';
            position: absolute;
            inset: 0;
            border-radius: 50%;
            border: 2px solid transparent;
            transition: border-color 0.3s;
        }

        .team-card:hover .team-photo::after {
            border-color: var(--accent);
        }

        .team-photo img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .team-name {
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.75rem;
            margin-bottom: 0.25rem;
        }

        .team-role {
            font-size: 0.75rem;
            color: var(--text-muted);
            letter-spacing: 0.15em;
            text-transform: uppercase;
            margin-bottom: 1rem;
        }

        .team-phone {
            font-size: 1.125rem;
            color: var(--accent);
            transition: opacity 0.3s;
        }

        .team-phone:hover {
            opacity: 0.7;
        }

        /* ========== PRODUCTION — Asymmetric Gallery ========== */
        .production {
            padding: 8rem 2rem;
            background: var(--bg-medium);
        }

        .production-inner {
            max-width: 1400px;
            margin: 0 auto;
        }

        .production-header {
            max-width: 600px;
            margin-bottom: 4rem;
        }

        .production h2 {
            font-family: 'Cormorant Garamond', serif;
            font-size: clamp(2rem, 5vw, 3rem);
            margin-bottom: 1.5rem;
        }

        .production-text {
            font-size: 1.0625rem;
            color: var(--text-secondary);
            line-height: 1.8;
        }

        .production-gallery {
            display: grid;
            grid-template-columns: 1fr;
            gap: 1rem;
        }

        .production-item {
            overflow: hidden;
            position: relative;
        }

        .production-item img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.6s;
        }

        .production-item:hover img {
            transform: scale(1.05);
        }

        .production-item--large {
            aspect-ratio: 16/9;
        }

        .production-item--small {
            aspect-ratio: 4/3;
        }

        /* ========== SAFETY — Dramatic Split ========== */
        .safety {
            padding: 8rem 2rem;
            background: var(--bg);
        }

        .safety-inner {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            gap: 4rem;
            align-items: center;
        }

        .safety-content h2 {
            font-family: 'Cormorant Garamond', serif;
            font-size: clamp(2rem, 5vw, 3rem);
            margin-bottom: 2rem;
        }

        .safety-text {
            font-size: 1.0625rem;
            color: var(--text-secondary);
            line-height: 1.8;
            margin-bottom: 2rem;
        }

        .safety-list {
            list-style: none;
        }

        .safety-list li {
            position: relative;
            padding: 1rem 0 1rem 2rem;
            font-size: 1rem;
            color: var(--text);
            border-bottom: 1px solid var(--border);
        }

        .safety-list li:first-child {
            border-top: 1px solid var(--border);
        }

        .safety-list li::before {
            content: '→';
            position: absolute;
            left: 0;
            color: var(--accent);
        }

        .safety-cert {
            text-align: center;
        }

        .safety-cert-wrap {
            position: relative;
            display: inline-block;
            padding: 2rem;
            background: var(--bg-light);
            border: 1px solid var(--border);
        }

        .safety-cert-wrap::before {
            content: 'ГОСТ Р';
            position: absolute;
            top: -0.75rem;
            left: 50%;
            transform: translateX(-50%);
            background: var(--bg);
            padding: 0 1rem;
            font-size: 0.6875rem;
            letter-spacing: 0.2em;
            color: var(--accent);
        }

        .safety-cert img {
            max-width: 250px;
            cursor: pointer;
            transition: transform 0.3s;
        }

        .safety-cert img:hover {
            transform: scale(1.02);
        }

        .zoom-hint {
            margin-top: 1rem;
            font-size: 0.75rem;
            color: var(--text-muted);
        }

        /* ========== CONTACTS — Bold Phone ========== */
        .contacts {
            padding: 10rem 2rem;
            background: var(--bg-medium);
            text-align: center;
        }

        .contacts-inner {
            max-width: 800px;
            margin: 0 auto;
        }

        .contacts h2 {
            font-family: 'Cormorant Garamond', serif;
            font-size: clamp(1.5rem, 4vw, 2.5rem);
            margin-bottom: 3rem;
        }

        .contacts-phone {
            display: block;
            font-family: 'Cormorant Garamond', serif;
            font-size: clamp(3rem, 10vw, 7rem);
            font-weight: 400;
            color: var(--text);
            line-height: 1;
            margin-bottom: 1rem;
            transition: color 0.3s;
        }

        .contacts-phone:hover {
            color: var(--accent);
        }

        .contacts-hint {
            font-size: 0.875rem;
            color: var(--text-muted);
            margin-bottom: 4rem;
        }

        .contacts-details {
            display: grid;
            gap: 2rem;
            text-align: left;
            margin-top: 4rem;
            padding-top: 4rem;
            border-top: 1px solid var(--border);
        }

        .contacts-block h3 {
            font-size: 0.6875rem;
            font-weight: 500;
            letter-spacing: 0.2em;
            text-transform: uppercase;
            color: var(--accent);
            margin-bottom: 1rem;
        }

        .contacts-block address {
            font-style: normal;
            font-size: 1rem;
            color: var(--text-secondary);
            line-height: 1.8;
        }

        .contacts-social {
            display: flex;
            gap: 1rem;
            margin-top: 1rem;
        }

        .contacts-social a {
            width: 48px;
            height: 48px;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 1px solid var(--border);
            color: var(--text-secondary);
            transition: all 0.3s;
        }

        .contacts-social a:hover {
            border-color: var(--accent);
            color: var(--accent);
        }

        .contacts-req {
            display: grid;
            gap: 0.75rem;
        }

        .contacts-req-item {
            display: flex;
            justify-content: space-between;
            font-size: 0.875rem;
            padding-bottom: 0.75rem;
            border-bottom: 1px solid var(--border);
        }

        .contacts-req-label {
            color: var(--text-muted);
        }

        .contacts-req-value {
            color: var(--text-secondary);
            text-align: right;
        }

        /* ========== FOOTER ========== */
        .footer {
            padding: 2rem;
            text-align: center;
            font-size: 0.75rem;
            color: var(--text-muted);
            letter-spacing: 0.1em;
        }

        /* ========== MODAL ========== */
        .modal {
            position: fixed;
            inset: 0;
            z-index: 1000;
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s;
        }

        .modal.active {
            opacity: 1;
            visibility: visible;
        }

        .modal-overlay {
            position: absolute;
            inset: 0;
            background: rgba(0,0,0,0.95);
        }

        .modal-box {
            position: relative;
            background: var(--bg-light);
            border: 1px solid var(--border);
            padding: 3rem;
            max-width: 420px;
            width: 90%;
        }

        .modal-close {
            position: absolute;
            top: 1rem;
            right: 1rem;
            background: none;
            border: none;
            color: var(--text-muted);
            cursor: pointer;
            transition: color 0.3s;
        }

        .modal-close:hover { color: var(--text); }

        .modal h3 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.75rem;
            margin-bottom: 0.5rem;
        }

        .modal p {
            font-size: 0.9375rem;
            color: var(--text-secondary);
            margin-bottom: 2rem;
        }

        .modal input,
        .modal textarea {
            width: 100%;
            padding: 1rem;
            margin-bottom: 1rem;
            font-family: 'Outfit', sans-serif;
            font-size: 1rem;
            background: var(--bg);
            border: 1px solid var(--border);
            color: var(--text);
            transition: border-color 0.3s;
        }

        .modal input:focus,
        .modal textarea:focus {
            outline: none;
            border-color: var(--accent);
        }

        .modal textarea { min-height: 100px; resize: vertical; }

        .modal button[type="submit"] {
            width: 100%;
            padding: 1.25rem;
            background: var(--accent);
            border: none;
            color: var(--bg);
            font-family: 'Outfit', sans-serif;
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
            transition: opacity 0.3s;
        }

        .modal button[type="submit"]:hover { opacity: 0.9; }

        .modal-privacy {
            margin-top: 1rem;
            font-size: 0.75rem;
            color: var(--text-muted);
            text-align: center;
        }

        .modal-privacy a { color: var(--accent); }

        .modal-success {
            text-align: center;
            padding: 2rem 0;
        }

        .modal-success-icon {
            width: 64px;
            height: 64px;
            margin: 0 auto 1.5rem;
            background: var(--accent);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            color: var(--bg);
        }

        /* ========== LIGHTBOX ========== */
        .lightbox {
            position: fixed;
            inset: 0;
            z-index: 1000;
            display: flex;
            align-items: center;
            justify-content: center;
            background: rgba(0,0,0,0.98);
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s;
        }

        .lightbox.active {
            opacity: 1;
            visibility: visible;
        }

        .lightbox img {
            max-width: 90%;
            max-height: 90vh;
        }

        .lightbox-close {
            position: absolute;
            top: 2rem;
            right: 2rem;
            background: none;
            border: none;
            color: var(--text);
            cursor: pointer;
        }

        /* ========== RESPONSIVE ========== */
        @media (min-width: 640px) {
            .header-nav { display: flex; }
            .team-grid { grid-template-columns: repeat(2, 1fr); }
            .production-gallery {
                grid-template-columns: repeat(2, 1fr);
            }
            .contacts-details { grid-template-columns: repeat(2, 1fr); }
        }

        @media (min-width: 768px) {
            .founder-inner { grid-template-columns: 1fr 1.2fr; }
            .founder-text { columns: 2; }
            .safety-inner { grid-template-columns: 1fr 1fr; }
        }

        @media (min-width: 1024px) {
            .production-gallery {
                grid-template-columns: 2fr 1fr 1fr;
                grid-template-rows: auto auto;
            }
            .production-item--large {
                grid-row: span 2;
            }
            .contacts-details { grid-template-columns: repeat(3, 1fr); }
        }

        /* Reduce motion */
        @media (prefers-reduced-motion: reduce) {
            .stats-track { animation: none; }
            * { transition-duration: 0.01ms !important; }
        }
    </style>
</head>
<body>
    <header class="header" id="header">
        <div class="header-inner">
            <a href="/test/v4" class="logo">С ЛЁГКИМ ПАРОМ</a>
            <nav class="header-nav">
                <a href="/test/v4#collection">Проекты</a>
                <a href="/test/v4#craft">Доставка</a>
                <a href="/test/about-2">О компании</a>
                <a href="#contacts">Контакты</a>
            </nav>
            <a href="tel:88003500393" class="header-phone">8-800-350-03-93</a>
        </div>
    </header>

    <!-- HERO -->
    <section class="hero">
        <div class="hero-content">
            <div class="hero-eyebrow">О компании</div>
            <h1 class="serif">
                <span>С 2014 года создаём</span>
                <span>бани, которые служат</span>
                <span class="highlight">долгие годы</span>
            </h1>
            <div class="hero-sub">
                <p>Производство в Пестово, Новгородская область. Доставка по всей России.</p>
            </div>
        </div>
    </section>

    <!-- STATS -->
    <section class="stats">
        <div class="stats-track">
            <div class="stat-item">
                <span class="stat-number">800K</span>
                <span class="stat-label">километров доставки</span>
            </div>
            <div class="stat-item">
                <span class="stat-number">10</span>
                <span class="stat-label">лет на рынке</span>
            </div>
            <div class="stat-item">
                <span class="stat-number">30+</span>
                <span class="stat-label">бань в месяц</span>
            </div>
            <div class="stat-item">
                <span class="stat-number">538</span>
                <span class="stat-label">единиц техники</span>
            </div>
            <div class="stat-item">
                <span class="stat-number">150+</span>
                <span class="stat-label">видео на YouTube</span>
            </div>
            <!-- Duplicate for seamless loop -->
            <div class="stat-item">
                <span class="stat-number">800K</span>
                <span class="stat-label">километров доставки</span>
            </div>
            <div class="stat-item">
                <span class="stat-number">10</span>
                <span class="stat-label">лет на рынке</span>
            </div>
            <div class="stat-item">
                <span class="stat-number">30+</span>
                <span class="stat-label">бань в месяц</span>
            </div>
            <div class="stat-item">
                <span class="stat-number">538</span>
                <span class="stat-label">единиц техники</span>
            </div>
            <div class="stat-item">
                <span class="stat-number">150+</span>
                <span class="stat-label">видео на YouTube</span>
            </div>
        </div>
    </section>

    <!-- FOUNDER -->
    <section class="founder">
        <div class="founder-inner">
            <div class="founder-photo-wrap">
                <div class="founder-photo">
                    <img src="/images/about/founder.webp" alt="Георгий Дзамуков" loading="lazy">
                </div>
                <div class="founder-label-box">
                    <span>Основатель</span>
                    <strong>Георгий Дзамуков</strong>
                </div>
            </div>
            <div class="founder-content">
                <blockquote class="founder-quote">
                    Делать так, чтобы клиент был доволен через 5 лет, а не только в день покупки.
                </blockquote>
                <div class="founder-text">
                    <p>Привет! Меня зовут Георгий, и я основал «С лёгким паром» в 2014 году.</p>
                    <p>Я вырос в Пестово — небольшом городе в Новгородской области, где каждый второй умеет работать с деревом. После учёбы в Петербурге на инженера-строителя вернулся домой и открыл производство бань.</p>
                    <p>Большинство производителей гонятся за быстрой прибылью. Мы выбрали другой путь — качество на долгие годы.</p>
                    <p>У нас открытый YouTube-канал с 150+ видео. Комментарии не удаляем — там много отзывов от клиентов спустя годы эксплуатации. Посмотрите сами и примите взвешенное решение.</p>
                    <p class="founder-signature">Пусть наши бани радуют вас! С лёгким паром!</p>
                </div>
                <button type="button" class="founder-btn" id="contactBtn">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
                    </svg>
                    Написать Георгию лично
                </button>
            </div>
        </div>
    </section>

    <!-- TIMELINE -->
    <section class="timeline">
        <div class="timeline-inner">
            <h2 class="serif">Наш путь</h2>
            <div class="timeline-line">
                <div class="timeline-item">
                    <div class="timeline-year">2014</div>
                    <p class="timeline-text">Георгий Дзамуков основал компанию «С лёгким паром» в родном Пестово. Первые бани собирали вручную небольшой командой.</p>
                </div>
                <div class="timeline-item">
                    <div class="timeline-year">2016</div>
                    <p class="timeline-text">Вышли на стабильное производство. Установили регламенты и чек-лист проверки бань.</p>
                </div>
                <div class="timeline-item">
                    <div class="timeline-year">2018</div>
                    <p class="timeline-text">Открыли YouTube-канал. Более 150 видео обзоров и отзывов.</p>
                </div>
                <div class="timeline-item">
                    <div class="timeline-year">2020</div>
                    <p class="timeline-text">Получили сертификат ГОСТ Р добровольно — безопасность превыше всего.</p>
                </div>
                <div class="timeline-item">
                    <div class="timeline-year">2024</div>
                    <p class="timeline-text">10 лет на рынке. 5000+ бань. 34 сотрудника. Продолжаем расти.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- TEAM -->
    <section class="team">
        <div class="team-inner">
            <h2 class="serif">Люди, которые помогут вам</h2>
            <div class="team-grid">
                <div class="team-card">
                    <div class="team-photo">
                        <img src="/images/about/team-tatiana.webp" alt="Татьяна" loading="lazy">
                    </div>
                    <div class="team-info">
                        <h3 class="team-name serif">Татьяна</h3>
                        <div class="team-role">Менеджер по логистике</div>
                        <a href="tel:89319813301" class="team-phone">8 931 981-33-01</a>
                    </div>
                </div>
                <div class="team-card">
                    <div class="team-photo">
                        <img src="/images/about/team-ivan.webp" alt="Иван" loading="lazy">
                    </div>
                    <div class="team-info">
                        <h3 class="team-name serif">Иван</h3>
                        <div class="team-role">Менеджер по продажам</div>
                        <a href="tel:89811630545" class="team-phone">8 981 163-05-45</a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- PRODUCTION -->
    <section class="production">
        <div class="production-inner">
            <div class="production-header">
                <h2 class="serif">Наше производство в Пестово</h2>
                <p class="production-text">Все бани собираются на собственном производстве в городе Пестово, Новгородская область. Здесь работают 34 человека — плотники, сборщики, контролёры качества.</p>
            </div>
            <div class="production-gallery">
                <div class="production-item production-item--large">
                    <img src="/images/about/production-1.webp" alt="Производство бань" loading="lazy">
                </div>
                <div class="production-item production-item--small">
                    <img src="/images/about/production-3.webp" alt="Команда" loading="lazy">
                </div>
                <div class="production-item production-item--small">
                    <img src="/images/about/production-2.webp" alt="Готовые бани" loading="lazy">
                </div>
            </div>
        </div>
    </section>

    <!-- SAFETY -->
    <section class="safety">
        <div class="safety-inner">
            <div class="safety-content">
                <h2 class="serif">Наши бани — безопасные</h2>
                <p class="safety-text">Закон не требует сертификации бань. Но мы получили сертификат ГОСТ Р добровольно — потому что отвечаем за каждое изделие.</p>
                <h3 style="font-size: 0.75rem; letter-spacing: 0.15em; text-transform: uppercase; color: var(--text-muted); margin-bottom: 1.5rem;">Что это значит</h3>
                <ul class="safety-list">
                    <li>Печь изолирована от деревянных поверхностей</li>
                    <li>Дымоход выведен через специальный короб из негорючего материала</li>
                    <li>Электропроводка соответствует нормам пожарной безопасности</li>
                </ul>
            </div>
            <div class="safety-cert">
                <div class="safety-cert-wrap">
                    <img src="/images/about/certificate.webp" alt="Сертификат ГОСТ Р" id="certImg" loading="lazy">
                </div>
                <p class="zoom-hint">Нажмите для увеличения</p>
            </div>
        </div>
    </section>

    <!-- CONTACTS -->
    <section class="contacts" id="contacts">
        <div class="contacts-inner">
            <h2 class="serif">Контакты и реквизиты</h2>
            <a href="tel:88003500393" class="contacts-phone">8-800-350-03-93</a>
            <p class="contacts-hint">Бесплатно по России · 8:00–24:00</p>

            <div class="contacts-details">
                <div class="contacts-block">
                    <h3>Производство</h3>
                    <address>174510, Новгородская обл.,<br>г. Пестово, ул. Дачная, 5</address>
                    <div class="contacts-social">
                        <a href="#" title="YouTube">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z"/>
                            </svg>
                        </a>
                        <a href="https://t.me/bani_1" title="Telegram">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm4.64 6.8c-.15 1.58-.8 5.42-1.13 7.19-.14.75-.42 1-.68 1.03-.58.05-1.02-.38-1.58-.75-.88-.58-1.38-.94-2.23-1.5-.99-.65-.35-1.01.22-1.59.15-.15 2.71-2.48 2.76-2.69a.2.2 0 00-.05-.18c-.06-.05-.14-.03-.21-.02-.09.02-1.49.95-4.22 2.79-.4.27-.76.41-1.08.4-.36-.01-1.04-.2-1.55-.37-.63-.2-1.12-.31-1.08-.66.02-.18.27-.36.74-.55 2.92-1.27 4.86-2.11 5.83-2.51 2.78-1.16 3.35-1.36 3.73-1.36.08 0 .27.02.39.12.1.08.13.19.14.27-.01.06.01.24 0 .37z"/>
                            </svg>
                        </a>
                        <a href="#" title="ВКонтакте">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M15.684 0H8.316C1.592 0 0 1.592 0 8.316v7.368C0 22.408 1.592 24 8.316 24h7.368C22.408 24 24 22.408 24 15.684V8.316C24 1.592 22.408 0 15.684 0zm3.692 17.123h-1.744c-.66 0-.864-.525-2.05-1.727-1.033-1-1.49-1.135-1.744-1.135-.356 0-.458.102-.458.593v1.575c0 .424-.135.678-1.253.678-1.846 0-3.896-1.118-5.335-3.202C4.624 10.857 4 8.684 4 8.253c0-.254.102-.491.593-.491h1.744c.44 0 .61.203.78.678.863 2.49 2.303 4.675 2.896 4.675.22 0 .322-.102.322-.66V9.721c-.068-1.186-.695-1.287-.695-1.71 0-.203.17-.407.44-.407h2.744c.373 0 .508.203.508.643v3.473c0 .372.17.508.271.508.22 0 .407-.136.813-.542 1.254-1.406 2.151-3.574 2.151-3.574.119-.254.322-.491.763-.491h1.744c.525 0 .644.27.525.643-.22 1.017-2.354 4.031-2.354 4.031-.186.305-.254.44 0 .78.186.254.796.779 1.203 1.253.745.847 1.32 1.558 1.473 2.05.17.49-.085.744-.576.744z"/>
                            </svg>
                        </a>
                    </div>
                </div>

                <div class="contacts-block">
                    <h3>Реквизиты</h3>
                    <div class="contacts-req">
                        <div class="contacts-req-item">
                            <span class="contacts-req-label">ИП</span>
                            <span class="contacts-req-value">Дзамуков Г.М.</span>
                        </div>
                        <div class="contacts-req-item">
                            <span class="contacts-req-label">ИНН</span>
                            <span class="contacts-req-value">531312915054</span>
                        </div>
                        <div class="contacts-req-item">
                            <span class="contacts-req-label">ОГРНИП</span>
                            <span class="contacts-req-value">319532100003969</span>
                        </div>
                    </div>
                </div>

                <div class="contacts-block">
                    <h3>Банк</h3>
                    <div class="contacts-req">
                        <div class="contacts-req-item">
                            <span class="contacts-req-label">Банк</span>
                            <span class="contacts-req-value">Точка</span>
                        </div>
                        <div class="contacts-req-item">
                            <span class="contacts-req-label">Р/с</span>
                            <span class="contacts-req-value">...002442</span>
                        </div>
                        <div class="contacts-req-item">
                            <span class="contacts-req-label">БИК</span>
                            <span class="contacts-req-value">044525104</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <footer class="footer">
        © 2014–2025 С лёгким паром · Пестово, Новгородская область
    </footer>

    <!-- MODAL -->
    <div class="modal" id="modal">
        <div class="modal-overlay"></div>
        <div class="modal-box">
            <button class="modal-close" aria-label="Закрыть">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M18 6L6 18M6 6l12 12"/>
                </svg>
            </button>
            <h3 class="serif">Написать Георгию</h3>
            <p>Оставьте сообщение — Георгий ответит лично</p>
            <form id="contactForm">
                <input type="tel" id="phone" placeholder="+7 (___) ___-__-__" required>
                <textarea placeholder="Ваше сообщение..."></textarea>
                <button type="submit">Отправить</button>
                <p class="modal-privacy">Нажимая кнопку, вы соглашаетесь с <a href="/privacy">политикой конфиденциальности</a></p>
            </form>
            <div class="modal-success" id="success" hidden>
                <div class="modal-success-icon">✓</div>
                <h4 class="serif">Отправлено!</h4>
                <p>Георгий свяжется с вами</p>
            </div>
        </div>
    </div>

    <!-- LIGHTBOX -->
    <div class="lightbox" id="lightbox">
        <img src="/images/about/certificate.webp" alt="Сертификат">
        <button class="lightbox-close" aria-label="Закрыть">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M18 6L6 18M6 6l12 12"/>
            </svg>
        </button>
    </div>

    <script>
    (function() {
        // Modal
        const modal = document.getElementById('modal');
        const btn = document.getElementById('contactBtn');
        const form = document.getElementById('contactForm');
        const success = document.getElementById('success');

        btn?.addEventListener('click', () => {
            modal.classList.add('active');
            document.body.style.overflow = 'hidden';
        });

        modal?.addEventListener('click', (e) => {
            if (e.target.classList.contains('modal-overlay') || e.target.closest('.modal-close')) {
                modal.classList.remove('active');
                document.body.style.overflow = '';
            }
        });

        form?.addEventListener('submit', async (e) => {
            e.preventDefault();
            const submitBtn = form.querySelector('button[type="submit"]');
            submitBtn.disabled = true;
            submitBtn.textContent = 'Отправка...';

            try {
                await fetch('/api/contact', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        phone: document.getElementById('phone')?.value,
                        message: form.querySelector('textarea')?.value || '',
                        source: 'about-2'
                    })
                });
                form.hidden = true;
                success.hidden = false;
                setTimeout(() => {
                    modal.classList.remove('active');
                    document.body.style.overflow = '';
                }, 2500);
            } catch (err) {
                submitBtn.disabled = false;
                submitBtn.textContent = 'Отправить';
            }
        });

        // Lightbox
        const lightbox = document.getElementById('lightbox');
        const certImg = document.getElementById('certImg');

        certImg?.addEventListener('click', () => {
            lightbox.classList.add('active');
            document.body.style.overflow = 'hidden';
        });

        lightbox?.addEventListener('click', () => {
            lightbox.classList.remove('active');
            document.body.style.overflow = '';
        });

        // Escape key
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape') {
                modal?.classList.remove('active');
                lightbox?.classList.remove('active');
                document.body.style.overflow = '';
            }
        });

        // Phone formatting
        const phoneInput = document.getElementById('phone');
        phoneInput?.addEventListener('input', function() {
            let v = this.value.replace(/\D/g, '');
            if (!v) v = '7';
            if (v[0] === '8') v = '7' + v.slice(1);
            if (v[0] !== '7') v = '7' + v;
            v = v.slice(0, 11);

            let f = '+7';
            if (v.length > 1) f += ' (' + v.slice(1, 4);
            if (v.length > 4) f += ') ' + v.slice(4, 7);
            if (v.length > 7) f += '-' + v.slice(7, 9);
            if (v.length > 9) f += '-' + v.slice(9);
            this.value = f;
        });

        // Header scroll effect
        const header = document.getElementById('header');
        window.addEventListener('scroll', () => {
            if (window.scrollY > 50) {
                header.classList.add('scrolled');
            } else {
                header.classList.remove('scrolled');
            }
        });
    })();
    </script>
</body>
</html>

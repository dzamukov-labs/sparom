---
// V3 - Bento Grid / Dashboard (Brooklyn style)
// All content in cards of different sizes, no classic sections
import BaseLayout from '../../layouts/BaseLayout.astro';

const projects = [
  { size: '2×3', title: 'Баня-бочка 2 метра', price: '364 900 ₽', img: '/images/proekty/2-3x3/main.webp', link: '/proekty/2-3x3/' },
  { size: '2.5×3', title: 'Баня-бочка 2.5 метра', price: '394 900 ₽', img: '/images/proekty/2-3x4/main.webp', link: '/proekty/2-3x4/' },
  { size: '3×3', title: 'Баня-бочка 3 метра', price: '409 900 ₽', img: '/images/banya-winter-3.png', link: '/proekty/2-3x5/' },
  { size: '3.5×3', title: 'Баня-бочка 3.5 метра', price: '449 900 ₽', img: '/images/proekty/2-3x6/main.webp', link: '/proekty/2-3x6/' },
  { size: '4×3', title: 'Баня-бочка 4 метра', price: '459 900 ₽', img: '/images/banya-winter-1.png', link: '/proekty/2-3x7/' },
  { size: '4.5×3', title: 'Баня-бочка 4.5 метра', price: '499 900 ₽', img: '/images/proekty/2-3x8/main.webp', link: '/proekty/2-3x8/' },
];

const reviews = [
  {
    name: 'Александр',
    location: 'Московская область',
    text: 'Заказывали баню-бочку 4 метра. Привезли и установили за один день, как и обещали. Качество отличное, париться одно удовольствие. Жена довольна, дети в восторге!',
    rating: 5
  },
  {
    name: 'Елена',
    location: 'Тверская область',
    text: 'Долго выбирали между разными производителями. Остановились на этой компании из-за честных цен и хороших отзывов. Не пожалели ни разу!',
    rating: 5
  },
  {
    name: 'Михаил',
    location: 'Владимирская область',
    text: 'Баня греется быстро, держит тепло хорошо. Уже год пользуемся, никаких проблем. Рекомендую всем соседям.',
    rating: 5
  }
];

const regions = ['Москва и МО', 'Санкт-Петербург и ЛО', 'Тверь', 'Рязань', 'Владимир', 'Калуга', 'Тула', 'Ярославль', 'Кострома', 'Иваново', 'Нижний Новгород', 'Смоленск', 'Брянск', 'Орёл', 'Курск', 'Воронеж', 'Липецк', 'Тамбов', 'Пенза', 'Саратов', 'Волгоград', 'Казань'];
---

<BaseLayout title="Бани-бочки под ключ — С лёгким паром">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;700&display=swap');

    :root {
      --bg: #0f0f0f;
      --card: #1a1a1a;
      --card-hover: #242424;
      --text: #ffffff;
      --text-muted: #888888;
      --accent: #22c55e;
      --accent-hover: #16a34a;
      --border: #2a2a2a;
      --gap: 16px;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Space Grotesk', sans-serif;
      background: var(--bg);
      color: var(--text);
      line-height: 1.5;
    }

    /* Main bento grid */
    .bento {
      display: grid;
      grid-template-columns: repeat(12, 1fr);
      gap: var(--gap);
      padding: var(--gap);
      max-width: 1600px;
      margin: 0 auto;
    }

    /* Card base styles */
    .card {
      background: var(--card);
      border-radius: 20px;
      padding: 24px;
      transition: all 0.3s ease;
      border: 1px solid var(--border);
      position: relative;
      overflow: hidden;
    }

    .card:hover {
      background: var(--card-hover);
      transform: translateY(-4px);
      box-shadow: 0 20px 40px rgba(0,0,0,0.4);
    }

    /* Hero card - spans full width */
    .card-hero {
      grid-column: span 12;
      min-height: 400px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      background: linear-gradient(135deg, var(--card) 0%, #1f2937 100%);
    }

    .card-hero h1 {
      font-size: clamp(2rem, 5vw, 3.5rem);
      font-weight: 700;
      line-height: 1.1;
      margin-bottom: 20px;
      max-width: 800px;
    }

    .card-hero p {
      font-size: 1.25rem;
      color: var(--text-muted);
      max-width: 600px;
      margin-bottom: 32px;
    }

    .btn-primary {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: var(--accent);
      color: var(--bg);
      padding: 16px 32px;
      border-radius: 12px;
      text-decoration: none;
      font-weight: 600;
      font-size: 1rem;
      transition: all 0.2s;
      width: fit-content;
    }

    .btn-primary:hover {
      background: var(--accent-hover);
      transform: scale(1.02);
    }

    /* Stats cards */
    .card-stat {
      grid-column: span 3;
      text-align: center;
      padding: 32px;
    }

    .stat-number {
      font-size: 3rem;
      font-weight: 700;
      color: var(--accent);
      margin-bottom: 8px;
    }

    .stat-label {
      color: var(--text-muted);
      font-size: 0.9rem;
    }

    /* Project cards */
    .card-project {
      grid-column: span 4;
      padding: 0;
      cursor: pointer;
      text-decoration: none;
      color: inherit;
    }

    .card-project:hover .project-img img {
      transform: scale(1.05);
    }

    .project-img {
      height: 200px;
      overflow: hidden;
      border-radius: 20px 20px 0 0;
    }

    .project-img img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.5s ease;
    }

    .project-info {
      padding: 20px;
    }

    .project-size {
      display: inline-block;
      background: var(--accent);
      color: var(--bg);
      padding: 4px 12px;
      border-radius: 20px;
      font-size: 0.8rem;
      font-weight: 600;
      margin-bottom: 8px;
    }

    .project-title {
      font-size: 1.1rem;
      font-weight: 500;
      margin-bottom: 8px;
    }

    .project-price {
      font-size: 1.25rem;
      font-weight: 700;
      color: var(--accent);
    }

    /* Benefits cards - different sizes */
    .card-benefit {
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .card-benefit-large {
      grid-column: span 6;
      min-height: 280px;
    }

    .card-benefit-small {
      grid-column: span 3;
    }

    .benefit-icon {
      font-size: 3rem;
      margin-bottom: 16px;
    }

    .benefit-value {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 8px;
    }

    .benefit-title {
      font-size: 1.1rem;
      font-weight: 500;
      margin-bottom: 8px;
    }

    .benefit-desc {
      color: var(--text-muted);
      font-size: 0.9rem;
    }

    /* Review cards */
    .card-review {
      grid-column: span 4;
    }

    .review-stars {
      color: #fbbf24;
      font-size: 1.2rem;
      margin-bottom: 16px;
    }

    .review-text {
      font-size: 1rem;
      line-height: 1.6;
      margin-bottom: 20px;
    }

    .review-author {
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .review-avatar {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      background: var(--border);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.2rem;
      font-weight: 600;
      color: var(--accent);
    }

    .review-name {
      font-weight: 500;
    }

    .review-location {
      color: var(--text-muted);
      font-size: 0.85rem;
    }

    /* Geography card */
    .card-geo {
      grid-column: span 8;
    }

    .card-geo h3 {
      font-size: 1.5rem;
      margin-bottom: 20px;
    }

    .regions-grid {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }

    .region-tag {
      background: var(--border);
      padding: 8px 16px;
      border-radius: 20px;
      font-size: 0.85rem;
      transition: all 0.2s;
    }

    .region-tag:hover {
      background: var(--accent);
      color: var(--bg);
    }

    /* CTA card */
    .card-cta {
      grid-column: span 4;
      background: linear-gradient(135deg, var(--accent) 0%, #059669 100%);
      display: flex;
      flex-direction: column;
      justify-content: center;
      text-align: center;
    }

    .card-cta:hover {
      background: linear-gradient(135deg, var(--accent-hover) 0%, #047857 100%);
    }

    .card-cta h3 {
      font-size: 1.5rem;
      margin-bottom: 16px;
      color: var(--bg);
    }

    .card-cta p {
      color: rgba(0,0,0,0.7);
      margin-bottom: 24px;
    }

    .btn-cta {
      background: var(--bg);
      color: var(--text);
      padding: 16px 32px;
      border-radius: 12px;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.2s;
      display: inline-block;
    }

    .btn-cta:hover {
      transform: scale(1.05);
    }

    /* Header card */
    .card-header {
      grid-column: span 12;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 16px 24px;
    }

    .card-header:hover {
      transform: none;
      box-shadow: none;
    }

    .logo {
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--text);
      text-decoration: none;
    }

    .logo span {
      color: var(--accent);
    }

    .header-phone {
      color: var(--text);
      text-decoration: none;
      font-weight: 500;
      font-size: 1.1rem;
    }

    .header-phone:hover {
      color: var(--accent);
    }

    /* Section title card */
    .card-title {
      grid-column: span 12;
      text-align: center;
      padding: 40px;
    }

    .card-title:hover {
      transform: none;
      box-shadow: none;
    }

    .card-title h2 {
      font-size: 2rem;
      font-weight: 700;
    }

    /* Footer card */
    .card-footer {
      grid-column: span 12;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 20px;
      padding: 32px;
    }

    .card-footer:hover {
      transform: none;
      box-shadow: none;
    }

    .footer-copy {
      color: var(--text-muted);
      font-size: 0.9rem;
    }

    .footer-links {
      display: flex;
      gap: 24px;
    }

    .footer-links a {
      color: var(--text-muted);
      text-decoration: none;
      transition: color 0.2s;
    }

    .footer-links a:hover {
      color: var(--accent);
    }

    /* Mobile responsive */
    @media (max-width: 1200px) {
      .card-stat { grid-column: span 6; }
      .card-project { grid-column: span 6; }
      .card-benefit-large { grid-column: span 12; }
      .card-benefit-small { grid-column: span 6; }
      .card-review { grid-column: span 6; }
      .card-geo { grid-column: span 12; }
      .card-cta { grid-column: span 12; }
    }

    @media (max-width: 768px) {
      :root {
        --gap: 12px;
      }

      .card-stat { grid-column: span 6; }
      .card-project { grid-column: span 12; }
      .card-benefit-large { grid-column: span 12; }
      .card-benefit-small { grid-column: span 12; }
      .card-review { grid-column: span 12; }

      .card-hero {
        min-height: 300px;
        padding: 32px 20px;
      }

      .card-hero h1 {
        font-size: 1.75rem;
      }

      .stat-number {
        font-size: 2rem;
      }

      .card-header {
        flex-direction: column;
        gap: 12px;
        text-align: center;
      }

      .card-footer {
        flex-direction: column;
        text-align: center;
      }

      .footer-links {
        flex-wrap: wrap;
        justify-content: center;
      }
    }
  </style>

  <div class="bento">
    <!-- Header -->
    <div class="card card-header">
      <a href="/" class="logo">С лёгким <span>паром</span></a>
      <a href="tel:88003500393" class="header-phone">8-800-350-03-93</a>
    </div>

    <!-- Hero -->
    <div class="card card-hero">
      <h1>Своя баня — это пятница, которую ждёшь всю неделю</h1>
      <p>Готовая баня на вашу дачу с доставкой и установкой за 1 день. Производим с 2014 года.</p>
      <a href="#projects" class="btn-primary">
        Выбрать баню
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
    </div>

    <!-- Stats -->
    <div class="card card-stat">
      <div class="stat-number">3000+</div>
      <div class="stat-label">бань установлено</div>
    </div>
    <div class="card card-stat">
      <div class="stat-number">600 000</div>
      <div class="stat-label">км доставки</div>
    </div>
    <div class="card card-stat">
      <div class="stat-number">10</div>
      <div class="stat-label">лет на рынке</div>
    </div>
    <div class="card card-stat">
      <div class="stat-number">150+</div>
      <div class="stat-label">видео с установок</div>
    </div>

    <!-- Section: Projects -->
    <div class="card card-title" id="projects">
      <h2>Наши проекты</h2>
    </div>

    <!-- Project cards -->
    {projects.map(project => (
      <a href={project.link} class="card card-project">
        <div class="project-img">
          <img src={project.img} alt={project.title} loading="lazy" />
        </div>
        <div class="project-info">
          <span class="project-size">{project.size} м</span>
          <div class="project-title">{project.title}</div>
          <div class="project-price">{project.price}</div>
        </div>
      </a>
    ))}

    <!-- Section: Benefits -->
    <div class="card card-title">
      <h2>Почему выбирают нас</h2>
    </div>

    <!-- Benefit cards - mixed sizes -->
    <div class="card card-benefit card-benefit-large">
      <div>
        <div class="benefit-icon">⏱️</div>
        <div class="benefit-value">1 час</div>
        <div class="benefit-title">Установка под ключ</div>
      </div>
      <div class="benefit-desc">Привозим готовую баню и устанавливаем на вашем участке. Вам остаётся только париться.</div>
    </div>

    <div class="card card-benefit card-benefit-small">
      <div class="benefit-icon">🏋️</div>
      <div class="benefit-value">5 тонн</div>
      <div class="benefit-title">Вес конструкции</div>
      <div class="benefit-desc">Не требует фундамента</div>
    </div>

    <div class="card card-benefit card-benefit-small">
      <div class="benefit-icon">💰</div>
      <div class="benefit-value">0₽</div>
      <div class="benefit-title">Доплат</div>
      <div class="benefit-desc">Цена фиксируется в договоре</div>
    </div>

    <div class="card card-benefit card-benefit-large">
      <div>
        <div class="benefit-icon">🎬</div>
        <div class="benefit-value">150+</div>
        <div class="benefit-title">Видео с установок</div>
      </div>
      <div class="benefit-desc">Снимаем каждую доставку и установку. Смотрите реальные отзывы клиентов на нашем YouTube канале.</div>
    </div>

    <!-- Section: Reviews -->
    <div class="card card-title">
      <h2>Отзывы клиентов</h2>
    </div>

    <!-- Review cards -->
    {reviews.map(review => (
      <div class="card card-review">
        <div class="review-stars">{'★'.repeat(review.rating)}</div>
        <p class="review-text">{review.text}</p>
        <div class="review-author">
          <div class="review-avatar">{review.name[0]}</div>
          <div>
            <div class="review-name">{review.name}</div>
            <div class="review-location">{review.location}</div>
          </div>
        </div>
      </div>
    ))}

    <!-- Geography + CTA -->
    <div class="card card-geo">
      <h3>География доставки</h3>
      <div class="regions-grid">
        {regions.map(region => (
          <span class="region-tag">{region}</span>
        ))}
      </div>
    </div>

    <div class="card card-cta">
      <h3>Закажите расчёт</h3>
      <p>Подберём баню под ваш бюджет и участок</p>
      <a href="/contacts" class="btn-cta">Оставить заявку</a>
    </div>

    <!-- Footer -->
    <div class="card card-footer">
      <div class="footer-copy">© 2014–2025 С лёгким паром. Производство бань-бочек.</div>
      <div class="footer-links">
        <a href="/about">О компании</a>
        <a href="/projects">Проекты</a>
        <a href="/delivery">Доставка</a>
        <a href="/contacts">Контакты</a>
      </div>
    </div>
  </div>
</BaseLayout>
